<!DOCTYPE html>
<html lang="pt-br">
	<head>
		<meta charset="utf-8">
		<title>
			Formul&aacute;rio de cadastro de Not&iacute;cia
		</title>
		<link rel="stylesheet" type="text/css" href="/css/bootstrap/bootstrap.min.css"/>
		<link rel="stylesheet" type="text/css" href="/css/style.css"/>
	</head>
	<body>
		<div class="container">
			<div class="row">
				<div class="col-md-12 col-xs-12 col-sm-12">
					<h1 class="title-page">
						Adicionar not&iacute;cia
					</h1>
				</div>
				<div class="col-md-6 col-md-offset-3 col-xs-12 col-sm-12">
				<% if(validation) { %>
					<% for(i = 0; i < validation.length; i ++) { %>
					<div class="alert alert-danger">
						<strong>
							Erro!
						</strong> Valor informado: <%= validation[i].value %> - <%= validation[i].msg %>
					</div>
					<% } %>
				<% } %>
					<form method="POST" action="/admin/news/add">
						<div class="form-group">
							<label>
								Nome autor:
							</label>
							<input type="text" class="form-control" required="required" name="name_author" placeholder="Nome do autor" value="<%= data.name_author %>">
						</div>
						<div class="form-group">
							<label>
								T&iacute;tulo:
							</label>
							<input type="text" class="form-control" required="required" name="title" placeholder="T&iacute;tulo da not&iacute;cia" value="<%= data.title %>">
						</div>
						<div class="form-group">
							<label>
								Resumo:
							</label>
							<input type="text" class="form-control" required="required" name="resume" placeholder="Resumo da not&iacute;cia" value="<%= data.resume %>">
						</div>
						<div class="form-group">
							<label>
								Conte&uacute;do:
							</label>
							<textarea name="content" class="form-control" required="required">
								<%=data.content %>
							</textarea>
						</div>
						<div class="form-group">
							<label>
								Data:
							</label>
							<input type="date" class="form-control" required="required" name="created" placeholder="Data da not&iacute;cia" value="<%= data.created %>">
						</div>
						<div class="form-group">
							<input type="submit" class="btn btn-success" value="Cadastrar">
						</div>
					</form>
				</div>
			</div>
		</div>
		<div class="container-fluid">
			<div class="row">
				<div class="footer">
					Desenvolvendo com NODEJS
				</div>
			</div>
		</div>
	</body>
</html>